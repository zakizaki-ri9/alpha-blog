<h1 align="center">
  <%= "Category: " + @category.name %>
</h1>

<!-- ページめくり用のコントロール -->
<div align="center">
  <!-- カテゴリに紐づく記事のリンクを表示 -->
  <%= will_paginate @category_articles %>
</div>

<%= render 'articles/article', obj: @category_articles %>

<!-- ページめくり用のコントロール -->
<div align="center">
  <!-- カテゴリに紐づく記事のリンクを表示 -->
  <%= will_paginate @category_articles %>
</div>
